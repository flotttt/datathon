# Copie ce fichier en .env puis renseigne ta cle (le .env n'est pas versionne).
#   cp .env.example .env
# Cle DeepSeek partagee par tous les agents (recuperee sur platform.deepseek.com).
DEEPSEEK_API_KEY=ta_cle_deepseek

# Optionnel : changer de modele / endpoint pour toute l'equipe
# LLM_MODEL=deepseek-v4-flash
# LLM_BASE_URL=https://api.deepseek.com
